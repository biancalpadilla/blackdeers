language: node_js
node_js:
- 4
sudo: required
os:
  - linux
  - osx
install:
- bundle install
- npm install -g grunt-cli
- npm install -g bigcommerce/stencil-cli
- npm install
script:
- grunt check
- stencil bundle
